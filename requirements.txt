# Compatibility entrypoint for environments that do not use uv.
# Keep these ranges aligned with pyproject.toml; production installs should use uv.lock.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
python-dotenv>=1.0,<2
httpx>=0.27,<1
cryptography>=42,<48
langchain-openai>=1.0,<2
langgraph>=1.0,<2
pytest>=8,<10
